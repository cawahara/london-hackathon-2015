* Run it on Ruby 2.2.3.

* Recommended to run with rbenv, rbenv-binstubs, and rbenv-gem-rehash.

* Run "bundle install --path vendor/bundle --binstubs vendor/bin" (in case of rbenv)

* Run "rake db:drop db:migrate db:seed" for demo initial data.

* Run "rails s"
